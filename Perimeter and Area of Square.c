#include<stdio.h>
int main()
{
int a;// a= length of the size of Square
printf("Enter side length of square : ");
scanf("%d",&a);
printf("Perimeter of Square = %d",4*a);// perimeter of Square = 4a
printf("\nArea of Square = %d",a*a);// area of Square = a*a
return 0;
}
